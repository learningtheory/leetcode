/*
<p>Given an n-ary tree, return the <i>preorder</i> traversal of its nodes&#39; values.</p>
&nbsp;

<p>For example, given a <code>3-ary</code> tree:</p>

<p><img height="40%" src="/static/images/problemset/NaryTreeExample.png" width="40%" /></p>
&nbsp;

<p>Return its preorder traversal as: <code>[1,3,5,6,2,4]</code>.</p>
&nbsp;

<p><b>Note:</b> Recursive solution is trivial, could you do it iteratively?</p><p>给定一个N叉树，返回其节点值的<em>前序遍历</em>。</p>

<p>&nbsp;</p>

<p>例如，给定一个&nbsp;<code>3叉树</code>&nbsp;:</p>

<p>&nbsp;</p>

<p><img src="/static/images/problemset/NaryTreeExample.png" style="height: 40%; width: 40%;"></p>

<p>&nbsp;</p>

<p>返回其前序遍历: <code>[1,3,5,6,2,4]</code>。</p>

<p>&nbsp;</p>

<p><strong>说明:&nbsp;</strong>递归法很简单，你可以使用迭代法完成此题吗?</p>
<p>给定一个N叉树，返回其节点值的<em>前序遍历</em>。</p>

<p>&nbsp;</p>

<p>例如，给定一个&nbsp;<code>3叉树</code>&nbsp;:</p>

<p>&nbsp;</p>

<p><img src="/static/images/problemset/NaryTreeExample.png" style="height: 40%; width: 40%;"></p>

<p>&nbsp;</p>

<p>返回其前序遍历: <code>[1,3,5,6,2,4]</code>。</p>

<p>&nbsp;</p>

<p><strong>说明:&nbsp;</strong>递归法很简单，你可以使用迭代法完成此题吗?</p>
*/


/*
// Definition for a Node.
class Node {
public:
    int val;
    vector<Node*> children;

    Node() {}

    Node(int _val, vector<Node*> _children) {
        val = _val;
        children = _children;
    }
};
*/
class Solution {
public:
    vector<int> preorder(Node* root) {
        
    }
};