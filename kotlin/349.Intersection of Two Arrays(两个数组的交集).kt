/**
<p>
Given two arrays, write a function to compute their intersection.
</p>

<p><b>Example:</b><br />
Given <i>nums1</i> = <code>[1, 2, 2, 1]</code>, <i>nums2</i> = <code>[2, 2]</code>, return <code>[2]</code>.
</p>

<p><b>Note:</b><br />
<ul>
<li>Each element in the result must be unique.</li>
<li>The result can be in any order.</li>
</ul>
</p><p>给定两个数组，写一个函数来计算它们的交集。</p>

<p><strong>例子:</strong></p>

<p>&nbsp;给定<em> num1</em>= <code>[1, 2, 2, 1]</code>, <em>nums2</em> = <code>[2, 2]</code>, 返回&nbsp;<code>[2]</code>.</p>

<p><strong>提示:</strong></p>

<ul>
	<li>每个在结果中的元素必定是唯一的。</li>
	<li>我们可以不考虑输出结果的顺序。</li>
</ul>
<p>给定两个数组，写一个函数来计算它们的交集。</p>

<p><strong>例子:</strong></p>

<p>&nbsp;给定<em> num1</em>= <code>[1, 2, 2, 1]</code>, <em>nums2</em> = <code>[2, 2]</code>, 返回&nbsp;<code>[2]</code>.</p>

<p><strong>提示:</strong></p>

<ul>
	<li>每个在结果中的元素必定是唯一的。</li>
	<li>我们可以不考虑输出结果的顺序。</li>
</ul>
**/


class Solution {
    fun intersection(nums1: IntArray, nums2: IntArray): IntArray {
        
    }
}